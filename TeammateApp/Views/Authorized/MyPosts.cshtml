@model List<Post>

<h3>Мои посты</h3>
<ul>
    @foreach (Post post in Model.OrderByDescending(e => e.Id))
    {
        <li>
            @post.Title <br /> 
            @post.Description
        </li>
    }
</ul>